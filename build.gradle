
apply plugin: PlatformPlugin

repositories {
	// First check local cache before accessing central repository
	// mavenRepo name:'Local', urls: "file://" + System.properties['user.home'] + "/.m2/repository"
	mavenLocal()
	mavenCentral()
	maven {
		url "http://repo.aduna-software.org/maven2/releases"
	}
	maven {
		url "http://fortytwo.net/maven2"
	}
	
	// ivy {   url "http://repo.mycompany.com/repo"}
}


platform {
	fetchSources = true
}

project.ext {
	
	//tinkerpopVersion='2.1.0'
	//sesameVersion='2.6.9'
	groovyVersion='2.1.5'
}

dependencies {
	platform "org.codehaus.groovy:groovy:${groovyVersion}"
	// "org.openrdf.sesame:sesame-sail-api:${sesameVersion}",
	platform 'org.slf4j:slf4j-api:1.7.5'
//	platform 'org.slf4j:slf4j-api:1.7.5:sources'
//	compile "org.neo4j:neo4j:1.8" 
	//"com.tinkerpop.blueprints:blueprints-core:${tinkerpopVersion}",
	//"com.tinkerpop:frames:${tinkerpopVersion}",
	//"com.tinkerpop.gremlin:gremlin-groovy:${tinkerpopVersion}",
	//"com.tinkerpop.blueprints:blueprints-neo4j-graph:${tinkerpopVersion}",
	platform "net.sf.opencsv:opencsv:2.3"
//"org.openrdf.sesame:sesame-runtime-osgi:${sesameVersion}"
//	"org.openrdf.sesame:sesame-repository-sail:${sesameVersion}",
//	"org.openrdf.sesame:sesame-sail-memory:${sesameVersion}"
	//"com.tinkerpop.blueprints:blueprints-sail-graph:${tinkerpopVersion}"
	
	//add sources for each dependency
//	configurations.compile.dependencies.each { dep ->
//		dep.artifact {
//		  group = dep.group
//		  name = dep.name
//		  version = dep.version
//		  classifier = 'sources'
//		  type = 'jar'
//		}
//	  }
}
/*
eclipse {
	//default settings for dependencies sources/javadoc download:
	downloadSources = true
	downloadJavadoc = true
}
*/